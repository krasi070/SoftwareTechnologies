@{
    ViewBag.Title = "ListFiles";
}

<h2>ListFiles</h2>
<form>
    Path: <input type="text"
                 name="path"/>
    <input type="submit"/>
</form>
@model List<string>
@foreach (var file in Model)
{
    <div>@file</div>
}
